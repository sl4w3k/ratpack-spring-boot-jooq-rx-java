apply plugin: 'java'
apply plugin: 'idea'

repositories {
    jcenter()
    mavenLocal()
}

ext {
    springVersion = '4.2.5.RELEASE'
}

subprojects {
    apply plugin: 'java'

    repositories {
        jcenter()
        mavenLocal()
    }
}